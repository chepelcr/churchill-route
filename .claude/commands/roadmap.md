---
description: Continue the world-editor → game-engine roadmap
---

Read `world-editor/docs/ENGINE_PLAN.md`. Take the first row marked `NEXT` in its
status table and implement it, following the working agreement in
`world-editor/CLAUDE.md`.

Move that row's status in the same commit as the work, and if the phase turned
out differently than the roadmap describes, edit the phase text too — a roadmap
that lies is worse than none.

If `$ARGUMENTS` names a phase, do that one instead of the first `NEXT`.
