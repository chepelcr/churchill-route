# El norte del mapa se conecta, y el puerto se pone a color

## Ya se puede llegar manejando al otro lado

Había media isla que se veía y no se llegaba. Medido sobre el mapa publicado, la
red de calles estaba partida en **69 pedazos**, y Pitahaya con toda la tierra
firme de esa orilla era uno entero — 163 mil celdas a las que sólo se llegaba por
agua. Las dos mitades se acercaban **en un solo punto de todo el mapa**: un corte
de 95 píxeles al final de la Calle del Arreo, donde dos calles se quedan a treinta
metros de tocarse porque el mapeador nunca las unió.

Ya está cosido. **68 pedazos, y el norte adentro del grande.**

No se hizo con una regla automática, y por una razón medida: hay 1 568 puntas de
calle sueltas en el mapa y 128 pares que se quedan a menos de cien píxeles.
Soldarlas todas habría inventado calles por todas partes, y sólo se habría notado
manejando.

## El pueblo se levanta

Los edificios dejaron de ser manchas planas. Ahora tienen **paredes**: el techo
se corre hacia afuera según lo lejos que esté del centro de la pantalla —como
cuando uno mira una ciudad desde un avión— y entre la huella y el techo se ve la
pared. Y cada pared sabe si le está dando el sol.

De paso se arregló algo que llevaba tiempo: la sombra de un edificio era una
copia de su huella corrida, y en un edificio alto se le despegaba dejando un
hueco por el que el sol no tiene cómo colarse. Ahora la sombra es el barrido
completo del cuerpo.

## Colores vivos, y el suelo con grano

El puerto estaba pintado en pasteles y ahora está pintado **a color**: el agua es
turquesa de verdad, el pasto es pasto, la arena es amarilla y las casas del
Centro son mango, coral, menta y lima — que es de lo que están pintadas. Y el
suelo dejó de ser un solo tono plano: la playa tiene grano, la calzada tiene
árido, el césped tiene brizna y el techo de zinc tiene desgaste.

## Cerros que se ven como cerros, y nubes que pasan

Tierra adentro el terreno **por fin se nota**: las laderas que dan al sol salen
claras y las contrarias oscuras, y la luz cruza de un lado al otro con la hora.
En la península no vas a ver nada, y eso es correcto — el arenal es plano de
verdad: el faro está a dos metros y medio.

Y sobre todo el mapa pasan **sombras de nubes**, empujadas por el viento. De
noche no hay ninguna, porque no hay sol que las proyecte.

## Adiós a las esquinas de noventa grados

Las parcelas y los edificios tienen las esquinas redondeadas. Suena a poco y
cambia mucho: un mapa hecho de datos vectoriales traza ángulos rectos perfectos y
eso es justo lo que lo hace ver rígido.

## Y cada barrio se ve como él mismo

El Cocal es arenal y madera. El Centro es cal y zinc. El Paseo son los pasteles
de un frente de balneario. Antes los doce barrios salían del mismo bombo de
colores.

---

## 🧾 Changelog

### El empalme del norte

- `content/world/geography.json` -> `roadLinks`, autorado **en geo** (la regla de
  la casa), con el motor en `churchill/world/service/roadlink.py`. El build
  resuelve la punta de calle real más cercana a cada ancla y emite la polilínea
  que faltaba, heredando clase y ancho de la vía que continúa.
- **Por qué no una regla general, medido**: 1 568 extremos libres, 128 pares bajo
  100 px, y sólo **9** comparten nombre — el de la Calle del Arreo no, porque sus
  dos puntas son anónimas. Una regla o se pierde la que importa o suelda 128.
- `finish.verify` exige que las dos puntas queden en la componente que **alcanza
  el spawn**. Es la invariante de alcanzabilidad que no existía: nada comprobaba
  que el mapa se pudiera recorrer.
- Resultado exacto: **69 -> 68 componentes**, la que queda con 4 391 519 celdas =
  4 228 466 + 162 945 + las 108 del corredor; 2 213 -> 2 214 vías; 9 tiles de
  1 000 cambiados.
- Tropiezo anotado: `reachM` ya lo autoraban `piers.json` y `railway.json` y lo
  convertían A MANO tres servicios, así que meterlo en `METRE_KEYS` —que borra la
  llave en metros— les habría quitado la llave de debajo y reventado el build a
  25 minutos de arrancar. Lo cazó `test_piers` antes de pagar la corrida.

### El aspecto

- `c2d/noise.js` — ruido de valor sembrado en el `hash01` que ya existía. Su
  amplitud la topa el RÁSTER (menos de una celda), porque la arena dibujada ES la
  arena y una costa ondulada de más mentiría sobre dónde está el agua.
- `c2d/curves.js` — **redondeo por esquina, y NO Chaikin**: Chaikin encoge el
  polígono entero y ahí nacen las costuras contra la calle de al lado. Aquí las
  aristas no se mueven. `roundedOutline` devuelve órdenes y `roundedPath` las
  vuelve `Path2D`, para poder medirlo en node y para que un WebGL futuro pueda
  teselarlo.
- `c2d/districts.js` + `materials.json -> districts` — paleta por barrio. No
  reimplementa `W.districtAt`, cachea la respuesta en el objeto, y **lo que un
  edificio ES gana sobre dónde está**: una iglesia es una iglesia en todas
  partes.
- `c2d/depth.js` — paralaje con la rotación de cámara gratis (se calcula en
  píxeles de mundo), aristas de silueta que sirven para las paredes Y para el
  barrido de la sombra, y orden de pintado por distancia radial descendente,
  porque no hay z-buffer y los edificios se pintaban en orden de tile.
- Perillas nuevas en `effects.json`: `organic` y `parallax`. **Un cero en
  cualquiera devuelve el cuadro anterior**, que es lo que hace la migración
  demostrable.

### Herramientas

- `pnpm smoke:curves` — sin navegador. Mide que el ruido no tenga retícula
  (salto máximo 0,0129 en medio píxel) y que el redondeo no mueva una arista.
- **`pnpm smoke:perf` ya exige algo**: topa el render en 4,0 ms y el sim en 2,0,
  con el desglose por fase en el fallo. Medía con cuidado y no afirmaba nada.
- Aprendido y anotado en CLAUDE.md: **las hojas de arte necesitan un servidor
  fresco por cada lado del diff**. Con uno vivo, editar un registro entre dos
  capturas dio `scenes` 15,8 % cambiada — mentira entera. Con uno fresco por
  lado: sólo `parcels` cambia (5,07 %) y las otras cinco salen idénticas. El
  piso de ruido de estas hojas es CERO.

### Pendiente de escoger con el ojo

- `effects.json -> parallax.cameraHeightM` decide entre tapete y ciudad
  isométrica. Queda en 80 m; `logs/depth-{150,80,45}m.png` son el mismo bloque a
  las tres alturas.

### Dos avisos del build que vienen de antes

- El `probeSea` de `geography.json` apunta a tierra (verificado contra el mundo
  anterior).
- El canal de la lancha se cierra bajo 80 px en 68 estaciones — `smoke:crossing`
  ya reportaba un mínimo de 26.

### El grano, y lo que costó averiguar cómo ponerlo

- `c2d/materials.js` + `materials.json -> textures` — la textura es **sólo la
  tinta, sobre transparente**, y se pinta ENCIMA del relleno de siempre. Así el
  color queda libre (lo mezclan el clima y la hora cada cuadro), la caché es por
  `nombre@escala`, y **una superficie sin entrada se ve exactamente como ayer**,
  que es lo que hace la migración demostrable superficie por superficie.
- Cada tinta es un `rgb` y una `a` aparte, no un `rgba()` armado: es la
  convención que `alphaColor` ya documenta, y es lo que deja a una mancha
  desvanecerse hasta alfa cero sin escribir un color dentro del intérprete —
  cosa que `tests/test_materials.py` prohíbe, y que cazó al primer intento.
- **UN RELLENO CON PATRÓN CUESTA ~10 ns POR PÍXEL, Y AHORA ESTÁ MEDIDO.** Con
  todo lo demás apagado, en el Centro a 1280x720: sin texturas **17,32 ms** de
  cuadro; sólo la arena 17,08 (una franja: gratis); sólo el asfalto 18,51; sólo
  el césped 18,66; sólo el techo 19,48; sólo la acera 19,90; **y sólo la tierra
  32,95** — casi el doble del cuadro entero. No es su tinta: es que cubre la
  pantalla. Bajarle el mosaico de 180 px a 48, o rasterizarlo a escala 1, la
  deja en 26,5; sigue costando 9 ms. **No hay perilla que arregle un relleno con
  patrón del tamaño del viewport.**
- Así que la tierra se dibuja como lo que es: unos discos suaves sobre una
  retícula GLOBAL recorrida sólo sobre el rectángulo visible, con el disco
  pre-renderizado a un sprite que se blitea. Es el patrón que `paintWoods` ya
  usaba para plantar un bosque de 270 M px² al precio de uno pequeño, y la
  contención es `surfaceAt`, que además excluye de una vez calles, arena y
  aceras. **32,95 -> 17,85 ms**, o sea 0,05 ms de render.
- La acera queda **apagada en el registro, con el número al lado**: su banda se
  traza por calle con `ancho + 2·acera`, así que en cada cruce se repinta sobre
  sí misma — 2,7 ms de un cuadro de 17,3, más que ninguna otra, por el grano de
  una franja que casi no se ve. Encenderla es quitar una línea.
- **Y el reloj del render no veía nada de esto.** Mientras el cuadro real pasaba
  de 17,3 a 32,9 ms, el render medido en JS subía de 1,24 a 1,42 — dentro de
  presupuesto y sin una sola alarma, porque lo que el navegador tarda en
  RASTERIZAR no está en ese reloj. `smoke:perf` afirma ahora también el cuadro
  entero (26 ms), y su mensaje de fallo dice dónde mirar.

### La paleta

- 51 valores de `materials.json` y `surfaces.json`, escritos **a mano y con el
  antes y el después impreso**. Un barrido de saturación global también satura
  las sombras, los tintes y el gris del concreto, que es justo donde se ve mal.
- Prueba de que no se derramó: de las **once hojas de arte, diez salen
  IDÉNTICAS** y sólo `parcels` cambia (4,32 %, delta máximo de canal 16), que es
  el verde nuevo y la brizna. Con servidor FRESCO por cada lado, porque un
  `:8734` con un ciclo de edición encima parte el grafo de módulos y miente.

### El relieve y las nubes

- `c2d/relief.js` consume por fin `residentElevationTiles` —construido hace
  tiempo y **sin un solo consumidor**, porque era para un compositor 3-D que se
  borró entero— y llena la ranura `effects.json -> terrainShadow`, documentada
  en CLAUDE.md y nunca escrita.
- **Es un `drawImage` de una imagen diminuta, no un relleno.** El sombreado es
  un valor por MUESTRA y la malla es gruesa: 32x32 alturas por tile de 2 500 px.
  Se escriben esas 1 024 muestras en un canvas de 32x32 y se estira sobre el
  tile con `imageSmoothingEnabled` — la interpolación bilineal la pone el
  navegador. Uno a cuatro dibujos por cuadro.
- **El descarte va por TILE, no por muestra, y eso costó medirlo.** Muestra a
  muestra la península NO sale llana: las curvas del IGN vienen cada 2 m y las
  muestras cada 26, así que cruzar una curva da una pendiente local del 7,7 %
  sobre terreno que sube 2,15 m en 800. Sombrear eso costaba **2,3 ms de cuadro
  al puerto entero** por dibujar algo invisible.
- `c2d/skycover.js` — y **no** es la capa en `multiply` a media resolución que
  pedía el plan. Una sombra de nube ES unos discos suaves, así que la pinta el
  MISMO verbo que las manchas del suelo, con su receta en otro registro: dos
  autorías componiendo un verbo del motor. Coste medido: **cero**.
- Tres cosas que costaron y quedan escritas: la nube deriva moviendo la
  RETÍCULA y no cada disco (moverlos sueltos da un hervidero); el paso de la
  retícula tiene que ser bastante menor que la vista, o toda la pantalla cae en
  una celda y las nubes aparecen de golpe; y «medianoche» sale del registro —
  en `simulation.json` la noche es 0,60-0,88 del día, así que 0,5 y 0,92 son
  atardecer, y pedirlas hizo fallar la prueba con el cielo perfectamente bien.
- Dos smokes nuevos, porque una captura no puede probar ninguna de las dos
  cosas que importan. `smoke:relief`: la península sale en **0 px** sombreados,
  el cerro del este en **96 %**, y la fracción de luz a la izquierda pasa de
  0,83 por la mañana a 0,44 por la tarde. `smoke:clouds`: hay sombra de día,
  **0 %** de noche, y el centroide de la tinta corre 23 px en 48 s.
- Las once hojas de arte salen IDÉNTICAS, que es lo que se espera de dos capas
  que sólo tocan el mundo. `smoke:perf` en **21,0 / 26 ms**.
